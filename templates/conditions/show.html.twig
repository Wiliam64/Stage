{% extends 'base.html.twig' %}

{% block title %}Conditions{% endblock %}

{% block body %}
    <h1>Conditions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ condition.id }}</td>
            </tr>
            <tr>
                <th>Key_condition</th>
                <td>{{ condition.keyCondition }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ condition.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('conditions_index') }}">back to list</a>

    <a href="{{ path('conditions_edit', {'id': condition.id}) }}">edit</a>

    {{ include('conditions/_delete_form.html.twig') }}
{% endblock %}
