{% extends 'base.html.twig' %}

{% block title %}States{% endblock %}

{% block body %}
    <h1>Statut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ state.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ state.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ state.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projects_show', {'id': projet.id}) }}" class="btn">Retour au projet</a>

    <a href="{{ path('states_edit', {'id': state.id, 'projetid': projet.id}) }}">Modifier</a>

    {{ include('states/_delete_form.html.twig') }}
{% endblock %}
